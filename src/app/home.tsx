import { SafeAreaView } from "react-native";
import GameScreen from "@/screens/GameScreen";

function Home() {
  return (
    <SafeAreaView>
      <GameScreen />
    </SafeAreaView>
  );
}

export default Home;
